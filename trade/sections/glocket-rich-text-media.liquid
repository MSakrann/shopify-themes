{{ 'glocket-sections.css' | asset_url | stylesheet_tag }}

{% style %}
  #GlocketRichTextMedia-{{ section.id }} {
    --glocket-pad-top: {{ section.settings.padding_top }}px;
    --glocket-pad-bottom: {{ section.settings.padding_bottom }}px;
  }
{% endstyle %}

<div
  id="GlocketRichTextMedia-{{ section.id }}"
  class="glocket-section glocket-rich-media color-{{ section.settings.color_scheme }} gradient"
>
  <div class="glocket-section__inner glocket-rich-media__grid{% if section.settings.media_position == 'right' %} glocket-rich-media__grid--media-right{% endif %}">
    <div class="glocket-rich-media__media glocket-media-fit--{{ section.settings.image_fit }}">
      {% if section.settings.image != blank %}
        {{
          section.settings.image
          | image_url: width: 1800
          | image_tag:
            class: 'glocket-rich-media__image',
            loading: 'lazy',
            sizes: '(min-width: 990px) 50vw, 100vw',
            widths: '375, 550, 750, 1100, 1500, 1800'
        }}
      {% elsif section.settings.video_url != blank %}
        <div class="glocket-rich-media__video">
          {{ section.settings.video_url | external_video_tag: loading: 'lazy' }}
        </div>
      {% else %}
        {{ 'image' | placeholder_svg_tag: 'glocket-rich-media__placeholder placeholder-svg' }}
      {% endif %}
    </div>

    <div class="glocket-rich-media__content">
      {% if section.settings.subheading != blank %}
        <p class="glocket-subheading glocket-rich-media__subheading">{{ section.settings.subheading | escape }}</p>
      {% endif %}
      {% if section.settings.heading != blank %}
        <h2 class="glocket-heading glocket-rich-media__heading">{{ section.settings.heading }}</h2>
      {% endif %}
      {% if section.settings.body != blank %}
        <div class="glocket-body glocket-rich-media__body rte">{{ section.settings.body }}</div>
      {% endif %}
      {% if section.settings.cta_label != blank %}
        <a
          class="glocket-btn"
          {% if section.settings.cta_link == blank %}
            role="link"
            aria-disabled="true"
          {% else %}
            href="{{ section.settings.cta_link }}"
          {% endif %}
        >
          {{ section.settings.cta_label | escape }}
        </a>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Glocket rich text media",
  "tag": "section",
  "class": "section-glocket-rich-text-media",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Tell your product story"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Designed for everyday use"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Body",
      "default": "<p>Share what makes this product useful, distinctive, and easy to use.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "select",
      "id": "image_fit",
      "label": "Image fit",
      "options": [
        {
          "value": "extend",
          "label": "Extend (show full image)"
        },
        {
          "value": "cover",
          "label": "Cover (crop to fill)"
        },
        {
          "value": "contain",
          "label": "Contain (fit inside)"
        },
        {
          "value": "fill",
          "label": "Fill (stretch)"
        }
      ],
      "default": "extend"
    },
    {
      "type": "video_url",
      "id": "video_url",
      "accept": ["youtube", "vimeo"],
      "label": "Video URL",
      "info": "Shown when no image is selected."
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "Button link"
    },
    {
      "type": "select",
      "id": "media_position",
      "label": "Media position",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 64
    }
  ],
  "presets": [
    {
      "name": "Glocket rich text media"
    }
  ]
}
{% endschema %}
